# PayPal Configuration
PAYPAL_SANDBOX_CLIENT_ID=your_paypal_sandbox_client_id
PAYPAL_SANDBOX_CLIENT_SECRET=your_paypal_sandbox_client_secret

# Optional: Domains for Fastlane component (comma-separated)
DOMAINS=localhost,pp-webv6-demos.onrender.com

# Environment (automatically set by Render)
NODE_ENV=production
PORT=10000